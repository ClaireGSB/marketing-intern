// src/dataProcessors/guidelineProcessor.ts

// NOT USING THIS RIGHT NOW SINCE GUIDELINES ARE NOW ONLY 1 PER SUBTYPE

// import { Guideline } from '../dataAccess';

// export function processGuidelines(guidelines: Guideline[]): string {
//   return guidelines.map(guideline => {
//     const tag = guideline.guideline_type || 'guideline';
//     return `<${tag}>${guideline.guideline_content}</${tag}>`;
//   }).join('\n');
// }
